# SciCalc Pro — 三轮迭代开发记录

## 项目概述

**应用名称**：SciCalc Pro — 面向理工科的在线科学计算平台  
**技术栈**：HTML/CSS/JS + math.js(CDN) + Chart.js(CDN)  
**目标用户**：工程、物理、数学专业学生  
**部署方式**：GitHub Pages（自动部署脚本 deploy_github.py）

---

## 第一轮迭代：基础架构 + 四大核心功能

### ① 输入提示词

```
综合使用skills，帮我开发一个面向理工领域的网页应用。要求：
- 科学计算器（支持三角函数、对数、指数、阶乘、模运算）
- 函数绘图（2D交互式图形，Chart.js）
- 矩阵运算（加法、减法、乘法、转置、行列式、逆矩阵）
- 单位转换（长度、质量、时间、温度、面积、体积、速度、压强、能量、功率、角度共11类）
- 深色/浅色主题切换、响应式设计、键盘快捷键支持
- 使用 math.js 和 Chart.js CDN，纯前端无需构建工具
- 目标部署到 GitHub Pages
```

### ② AI输出结果

创建了完整的8文件项目结构：

| 文件 | 功能 | 行数 |
|------|------|------|
| `index.html` | 单页应用骨架，4个标签面板 | ~130行 |
| `css/style.css` | CSS变量主题系统，响应式@640px | ~300行 |
| `js/calculator.js` | 表达式求值引擎 + 7行按钮网格 | ~210行 |
| `js/grapher.js` | Chart.js函数绘图，多函数叠加 | ~220行 |
| `js/matrix.js` | 6种矩阵运算，Unicode框线输出 | ~170行 |
| `js/converter.js` | 11类别单位转换，温度特殊处理 | ~230行 |
| `js/app.js` | 标签切换/主题/键盘快捷键 | ~90行 |
| `js/constants.js` | 33个物理数学常数数据 | ~50行 |

### ③ 发现的问题/不足

通过 Code Review（review skill）发现 **10个问题**：

| # | 严重度 | 问题 | 文件 |
|---|--------|------|------|
| 1 | 🔴 Blocking | `.display-result` 区域从不显示计算结果 | calculator.js:196 |
| 2 | 🔴 Blocking | 键盘操作 `/` 触发Firefox快速查找 | app.js:88-113 |
| 3 | 🟡 Should-fix | 隐式乘法不支持 `)func` 和 `πfunc` | calculator.js:139 |
| 4 | 🟡 Should-fix | `CONSTANTS` 对象完全未使用 | constants.js |
| 5 | 🟡 Should-fix | 无效函数表达式用 `alert()` 阻塞主线程 | grapher.js:121 |
| 6 | 🟡 Should-fix | Matrix textarea 缺少 `<label>` 关联 | index.html |
| 7 | 🟡 Should-fix | 温度转换显示无实际公式 | converter.js:206 |
| 8 | 🔵 Nit | `toExponential(6)` 使整数因子变丑陋 | converter.js:214 |
| 9 | 🔵 Nit | 缺少 `prefers-color-scheme` 检测 | style.css |
| 10 | 🔵 Nit | 模块以全局变量存储 | app.js:32-35 |

### ④ 修复策略与改进措施

- **#1 显示Bug**：重写 `evaluate()` — 设置 `_justEvaluated` 标志，结果直接写 `.display-result`，表达式清空让用户开始新输入
- **#2 键盘干扰**：统一对所有匹配按键调用 `e.preventDefault()`
- **#3 隐式乘法**：新增2条正则替换：`)func` → `)*func`，`πfunc` → `π*func`
- **#4 死代码**：在 `initButtons()` 后调用 `renderConstants()` 渲染33个常数到可折叠面板
- **#5 UX问题**：添加 `showError()` 方法，在 `#grapherColors` 显示内联错误，4秒自动消失
- **#6 可访问性**：用 `<label for="matrixA">` 包裹 `<h3>` 内容
- **#7 温度公式**：添加 `tempFormula()` 方法，返回实际转换公式（如 `°C = K − 273.15`）
- **#8 数字格式**：添加 `formatRatio()` — 整数用 `toString()`，大/小数才用指数
- **#9 主题偏好**：在 `App.init()` 中检测 `window.matchMedia('(prefers-color-scheme: dark)')`
- **#10 全局变量**：保留简洁性，单页应用无冲突风险，暂不重构

---

## 第二轮迭代：功能增强 + 交互优化

### ① 输入提示词

```
在迭代1基础上增强：
- 绘图区域添加预设函数快捷按钮（sin/cos/tan/x²/x³/1/x/√x/|x|/eˣ/ln/高斯/sin(1/x)）
- 利用 CONSTANTS 数据在计算器下方添加可展开的「理工常数参考」面板
- 图表添加悬停坐标跟踪显示
- 数值格式化优化（整数不用科学计数法）
```

### ② AI输出结果

- **预设函数按钮**：在 `index.html` 绘图区添加12个 pill 按钮，每个带 `data-fn` 属性
- **常数参考面板**：在计算器按钮下方添加 `<details>` 可折叠面板，用 `CSS Grid auto-fill` 布局33个常数的卡片，点击即插入表达式
- **图表坐标跟踪**：通过 `Chart.js onHover` 回调实时显示光标位置的 `(x, y)` 坐标
- **CSS增强**：`.preset-funcs` / `.preset-btn` / `.chart-coords` / `.constants-panel` / `.const-card` 等新组件样式
- **JS交互**：`grapher.js` 绑定预设按钮点击 → 自动填入函数并绘制；`calculator.js` 新增 `renderConstants()` 方法

### ③ 发现的问题/不足

| # | 问题 | 说明 |
|---|------|------|
| 1 | 常数面板在计算器面板内，滚动受限 | `max-height: 240px + overflow-y: auto` 已处理 |
| 2 | 预设函数缺少 x 范围自适应 | 用户需手动调整 `xMin/xMax` 以适配不同函数形状 |
| 3 | 绘图标签在深色主题下对比度略低 | `plot-tag` 文字始终白色，浅色函数（如黄色）可读性下降 |

### ④ 修复策略与改进措施

- **#1**：维持 `240px` 折叠滚动，经验证在桌面和移动端均可用
- **#2**：暂不处理；用户调整范围是预期交互。后续可考虑按函数自动设置合适范围
- **#3**：`plot-tag` 已添加背景色 `var(--btn-func-bg)` 作为后备，浅色系函数可通过设置更深的颜色序列改善

---

## 第三轮迭代：UI打磨 + 部署准备

### ① 输入提示词

```
最终打磨：
- 添加 favicon
- 添加 meta description 用于SEO/分享
- 标签切换添加淡入动画
- 按钮点击添加按压缩放反馈
- 准备 GitHub Pages 自动部署脚本
- 编写完整的 README.md
- 编写迭代记录文档
```

### ② AI输出结果

- **Favicon**：内嵌 SVG data URI，使用 🔬 emoji 作为图标
- **SEO**：添加 `<meta name="description">`
- **标签动画**：`.tab-panel.active` 添加 `@keyframes fadeIn`（0.25s 淡入 + 上移）
- **按钮反馈**：hover 时 `translateY(-1px) + box-shadow`，active 时 `scale(0.97) + 无阴影`
- **部署脚本**：`deploy_github.py` — 使用 GitHub API 自动创建仓库、批量上传文件、启用 Pages
- **README.md**：含功能列表、快速开始、技术栈、项目结构
- **迭代记录**：本文件

### ③ 发现的问题/不足

| # | 问题 | 说明 |
|---|------|------|
| 1 | Git 未安装在当前环境 | winget 安装超时，需要手动安装 Git 或使用 Python API 脚本 |
| 2 | 移动端矩阵面板 textarea 太窄 | 已通过 `flex-direction: column` 在 640px 断点处理 |
| 3 | 图表在标签切换后可能不响应 resize | `switchTab` 中已添加 `setTimeout(() => grapher.chart.resize(), 100)` |

### ④ 修复策略与改进措施

- **#1 Git安装**：提供 `deploy_github.py` Python脚本绕过Git，直接通过 GitHub API 创建仓库和提交文件
- **#2 移动端**：已验证 640px 断点正确处理，textarea 宽度足够
- **#3 图表resize**：已修复，`switchTab` 时延迟调用 `chart.resize()`

---

## 最终项目结构

```
scicalc-pro/
├── index.html             # 主应用入口 (142行)
├── README.md              # 项目说明
├── deploy_github.py       # 自动部署脚本
├── AGENTS.md              # 开发文档
├── css/
│   └── style.css          # 主题 + 响应式样式 (350行)
├── js/
│   ├── constants.js       # 33个物理/数学常数
│   ├── calculator.js      # 科学计算引擎
│   ├── grapher.js         # Chart.js 函数绘图
│   ├── matrix.js          # 矩阵运算
│   ├── converter.js       # 单位转换
│   └── app.js             # 主控制器
└── 迭代记录.md             # 三轮迭代完整记录
```

## 部署说明

运行自动部署（需 Python 3.6+）：
```bash
python deploy_github.py --token ghp_xxxx --username YOUR_USERNAME
```

或参考 `README.md` 中的手动部署步骤。

---

> 📝 本文档记录了 SciCalc Pro 从零到完整的三轮迭代全过程，包括每轮的提示词、AI输出、发现问题和修复措施。
